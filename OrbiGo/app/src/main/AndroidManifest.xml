<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ds14.darren.orbigo">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".helpers.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan|adjustResize">
        <service android:name=".services.GeofenceTransitionsIntentService" />

        <activity
            android:name=".activities.LoginActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyNoActionBarTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@style/FullyTranslucent"
            tools:replace="android:theme" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".broadcast_receivers.ConnectivityReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <service android:name=".services.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".services.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".activities.HomeActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.TouristProfileActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.ListBusinessActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.BTOBottomNavActivity"
            android:label="@string/title_activity_btobottom_nav"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.TimelineActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.BusinessProfileActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.EarningsActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.NearbyPoiActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.PoiDetailsActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.RestWithMapActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.AddBuddyActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.TripMembersActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.RouteMapActivity"
            android:label="@string/title_activity_route_map"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.MyTripsActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.SavedPlacesActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.MyFriendsActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyTheme" />
        <activity
            android:name=".activities.SplashTouristActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyNoActionBarTheme" />
        <activity android:name=".activities.SplashBusinessActivity"
            android:launchMode="singleTop"
            android:theme="@style/MyNoActionBarTheme"/>
    </application>

</manifest>